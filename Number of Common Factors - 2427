class Solution(object):
    def commonFactors(self, a, b):
        limit=min(abs(a),abs(b))
        fact=0
        for i in range(1,limit+1):
            if a%i==0 and b%i==0:
                fact=fact+1
        return fact

        
        